export const HARISCHANDRAGAD_TREK_DATA = {
    name: "Harishchandragad Trek",
    title: "Harishchandragad Trek — Maharashtra",
    state: "Maharashtra",
    region: "Western Ghats (Sahyadri), Ahmednagar district, Maharashtra",
    startingPoint: "Pachnai village (~860 m), reachable from Kalyan or Igatpuri",
    maxAltitude: "1,424 m (4,672 ft)",
    distance: "~8 km one way via Pachnai",
    duration: "1–2 days",
    difficulty: "Moderate to Difficult",
    bestSeason: "June–September (monsoon) and October–February (winter)",
    permits: "No permits required; forest department rules apply — carry a valid photo ID",

    overview: {
        location: "Harishchandragad lies in the Malshej region of the Western Ghats, at the junction of Ahmednagar, Thane, and Pune districts of Maharashtra. The fort massif rises to 1,424 m above the Konkan plains and has been a stronghold since the 6th century.",
        landscape: "Deccan-trap basalt cliffs, monsoon waterfalls, and high grassy plateaus define the route. From the top, trekkers look across the Taramati peak, ancient caves, and the enormous semicircular rock wall of Konkan Kada plunging toward the Konkan coast.",
        heritage: "Harishchandragad finds mention in scriptures dating to the 6th century and was later shaped by the Mughals and Marathas under Chhatrapati Shivaji Maharaj. The Harishchandreshwar temple, Kedareshwar cave shrine, and centuries-old water cisterns make it a living heritage site."
    },

    facts: [
        {
            title: "Trek Location",
            value: "Ahmednagar, Maharashtra",
            description: "Set in the Sahyadri range of the Western Ghats, near Malshej Ghat at the tri-junction of Ahmednagar, Thane, and Pune districts.",
            icon: "\ud83d\udccd"
        },
        {
            title: "Maximum Altitude",
            value: "1,424 m",
            description: "The Taramati summit of Harishchandragad stands at roughly 1,424 m (4,672 ft) above sea level.",
            icon: "\u26f0\ufe0f"
        },
        {
            title: "Distance",
            value: "~8 km One Way",
            description: "Around 8 km of walking each way on the shortest Pachnai route; longer approaches start from Khireshwar or Belpada.",
            icon: "\ud83e\udd3e"
        },
        {
            title: "Duration",
            value: "1–2 Days",
            description: "Fit trekkers ascend and descend in a single long day; most itineraries camp overnight beside the temple to explore at leisure.",
            icon: "\ud83d\uddd3\ufe0f"
        },
        {
            title: "Difficulty",
            value: "Moderate–Difficult",
            description: "A steady forest climb followed by rock-cut steps at Tolar Khind; trails turn slippery during heavy monsoon rain.",
            icon: "\ud83e\udd57"
        },
        {
            title: "Best Season",
            value: "Monsoon & Winter",
            description: "June–September brings waterfalls and emerald grasslands; October–February offers clear skies and the famous cloud-sea below Konkan Kada.",
            icon: "\ud83c\udf24\ufe0f"
        },
        {
            title: "Starting Point",
            value: "Pachnai Village",
            description: "The trailhead is Pachnai (~860 m), the nearest base village, reached by road from Kalyan, Igatpuri, or Kotul.",
            icon: "\ud83d\udea9"
        },
        {
            title: "Permits",
            value: "No Permit Needed",
            description: "No special trekking permits are required for Indian nationals — just carry a government photo ID and follow forest department rules.",
            icon: "\ud83d\udec2"
        }
    ],

    naturalFeatures: [
        {
            title: "Konkan Kada",
            text: "A gigantic semicircular rock overhang facing west toward the Konkan coast. Its concave brink creates vertical wind updrafts, rainbow rings in monsoon mist, and the famous view of the cloud-sea at sunset."
        },
        {
            title: "Harishchandreshwar Temple",
            text: "A remarkable 6th-century stone temple carved from a single rock, crowned by a 1.5 m tall trishul-like pinnacle — the spiritual heart of the fort plateau."
        },
        {
            title: "Kedareshwar Cave",
            text: "A water-filled cave shrine holding a Shivalinga ringed by four pillars representing the yugas; three have already crumbled, adding to its mystique."
        },
        {
            title: "Saptatirtha Pushkarni",
            text: "Ancient step-well cisterns near the temple whose cool water sustained fort garrisons for centuries and still serve trekkers today."
        },
        {
            title: "Waterfalls & Streams",
            text: "Monsoon rains drape the cliffs in seasonal waterfalls, and streams like the Ghattrajwadi cascade along the lower trail between Pachnai and Tolar Khind."
        },
        {
            title: "Grassland Plateaus & Pinnacles",
            text: "Wind-scoured meadows spread across the fort top, while pinnacles such as Kalbhairav guard the approaches — classic Sahyadri topography carved from Deccan basalt."
        }
    ],

    routeStages: [
        {
            step: 1,
            stage: "Kalyan → Pachnai Village",
            altitude: "~50 m → 860 m",
            description: "An early morning drive of roughly 150 km via Malshej Ghat or Kotul brings you to Pachnai, the quiet base village beneath the fort's northern face.",
            keyHighlight: "Village fields with the fort wall looming overhead."
        },
        {
            step: 2,
            stage: "Pachnai → Tolar Khind",
            altitude: "860 m → 1,100 m",
            description: "The trail climbs through fields and dense deciduous forest, crossing stream beds before reaching Tolar Khind, the notch where the real ascent begins.",
            keyHighlight: "Shaded forest walk with monsoon stream crossings."
        },
        {
            step: 3,
            stage: "Tolar Khind → Temple Plateau",
            altitude: "1,100 m → 1,380 m",
            description: "Rock-cut steps and iron railings aid the climb through the khind onto the vast grassy plateau, where the Harishchandreshwar temple greets arriving trekkers.",
            keyHighlight: "First sight of the ancient temple against the sky."
        },
        {
            step: 4,
            stage: "Plateau Exploration — Caves & Cisterns",
            altitude: "~1,380 m → 1,400 m",
            description: "Wander the plateau to Kedareshwar cave, Saptatirtha cisterns, and dozens of meditation caves; most trekkers camp near the temple tonight.",
            keyHighlight: "Sunset over Kedareshwar cave and centuries-old carvings."
        },
        {
            step: 5,
            stage: "Temple → Konkan Kada → Descent",
            altitude: "1,400 m → 1,424 m → 860 m",
            description: "Walk out to Konkan Kada for sunrise above the cloud-sea, then retrace the route down the rock-cut steps and forest trail to Pachnai.",
            keyHighlight: "Standing atop the legendary semicircular cliff edge."
        }
    ],

    gallery: [
        {
            title: "Konkan Kada Cliff",
            caption: "The semicircular concave overhang of Konkan Kada — Harishchandragad's world-famous cliff face above the Konkan plains.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/b/b6/Harishchandragad_Konkan_Kada.jpg/960px-Harishchandragad_Konkan_Kada.jpg",
            credit: "Photo: Super Trekkers / Wikimedia Commons / CC BY-SA 4.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Harishchandragad_Konkan_Kada.jpg"
        },
        {
            title: "Harishchandreshwar Temple",
            caption: "The ancient rock-cut temple crowning the fort plateau, its trishul-shaped spire rising over the Sahyadri ridges.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/7/73/Harishchandreshwar_temple.jpg/960px-Harishchandreshwar_temple.jpg",
            credit: "Photo: VinayakPhadatare / Wikimedia Commons / CC BY-SA 4.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Harishchandreshwar_temple.jpg"
        },
        {
            title: "Trail from Pachnai",
            caption: "The forested path climbing from Pachnai village towards Tolar Khind — the shortest and most popular approach to the fort.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/1/1f/Trail_from_Pachnai_to_Harishchandragad_%2833264929342%29.jpg/960px-Trail_from_Pachnai_to_Harishchandragad_%2833264929342%29.jpg",
            credit: "Photo: Vis M / Wikimedia Commons / CC BY-SA 2.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Trail_from_Pachnai_to_Harishchandragad_(33264929342).jpg"
        },
        {
            title: "View from the Fort Top",
            caption: "Rolling Western Ghats ridgelines unfolding beyond Harishchandragad's grassy plateau after the monsoon.",
            image: "https://upload.wikimedia.org/wikipedia/commons/9/9c/View_from_Harishchandragad.jpg",
            credit: "Photo: Pmohite / Wikimedia Commons / CC BY-SA 3.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:View_from_Harishchandragad.jpg"
        },
        {
            title: "Fort Landscape",
            caption: "Emerald grasslands and basalt outcrops sweeping across the Harishchandragad massif in the wet season.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/8/8a/Landscape_view_of_the_Harishchandragad.jpg/960px-Landscape_view_of_the_Harishchandragad.jpg",
            credit: "Photo: Sushpawar001 / Wikimedia Commons / CC BY-SA 4.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Landscape_view_of_the_Harishchandragad.jpg"
        },
        {
            title: "Kalbhairav Pinnacle",
            caption: "The needle-like Kalbhairav pinnacle guarding the approaches to Harishchandragad — classic Sahyadri rock architecture.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/5/5b/Kalbhairav_pinnacle_Scj.jpg/960px-Kalbhairav_pinnacle_Scj.jpg",
            credit: "Photo: Cj.samson / Wikimedia Commons / GFDL 1.2",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Kalbhairav_pinnacle_Scj.jpg"
        }
    ]
};

const FALLBACK_IMAGE = 'data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="400" height="250" viewBox="0 0 100 100"><rect width="100%" height="100%" fill="%231e293b"/><text x="50%" y="55%" fill="%2334d399" font-size="24" text-anchor="middle"></text></svg>';

function escapeHtml(value) {
    return String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');
}

export function renderTrekFacts(facts, container) {
    if (!container || !Array.isArray(facts)) return;

    container.innerHTML = facts.map(fact => `
        <article class="fact-card">
            <div class="fact-icon">${escapeHtml(fact.icon)}</div>
            <div class="fact-value">${escapeHtml(fact.value)}</div>
            <h3 class="fact-title">${escapeHtml(fact.title)}</h3>
            <p class="fact-desc">${escapeHtml(fact.description)}</p>
        </article>
    `).join('');
}

export function renderNaturalFeatures(features, container) {
    if (!container || !Array.isArray(features)) return;

    container.innerHTML = features.map(feature => `
        <article class="feature-card">
            <h3>${escapeHtml(feature.title)}</h3>
            <p>${escapeHtml(feature.text)}</p>
        </article>
    `).join('');
}

export function renderRouteStages(stages, container) {
    if (!container || !Array.isArray(stages)) return;

    container.innerHTML = stages.map(stage => `
        <div class="route-step-card" data-step="${stage.step}">
            <div class="step-badge">Stage ${stage.step}</div>
            <h3 class="step-stage">${escapeHtml(stage.stage)}</h3>
            <div class="step-altitude">📍 Altitude: ${escapeHtml(stage.altitude)}</div>
            <p class="step-desc">${escapeHtml(stage.description)}</p>
            <div class="step-highlight">✨ <strong>Highlight:</strong> ${escapeHtml(stage.keyHighlight)}</div>
        </div>
    `).join('');
}

export function renderGallery(gallery, container) {
    if (!container || !Array.isArray(gallery)) return;

    container.innerHTML = gallery.map((item, index) => `
        <figure class="gallery-card" data-index="${index}" role="button" tabindex="0" aria-label="View larger image: ${escapeHtml(item.title)}">
            <div class="gallery-img-wrapper">
                <img
                    src="${escapeHtml(item.image)}"
                    alt="${escapeHtml(item.title)}"
                    class="gallery-img"
                    loading="lazy"
                />
            </div>
            <figcaption class="gallery-caption">
                <h4>${escapeHtml(item.title)}</h4>
                <p>${escapeHtml(item.caption)}</p>
                <a class="gallery-credit" href="${escapeHtml(item.sourceUrl)}" target="_blank" rel="noopener noreferrer">${escapeHtml(item.credit)}</a>
            </figcaption>
        </figure>
    `).join('');

    container.querySelectorAll('.gallery-img').forEach(img => {
        img.addEventListener('error', () => {
            if (img.dataset.fallbackApplied) return;
            img.dataset.fallbackApplied = 'true';
            img.src = FALLBACK_IMAGE;
        }, { once: true });
    });
}

export function renderImageCredits(gallery, container) {
    if (!container || !Array.isArray(gallery)) return;

    container.innerHTML = gallery.map(item => `
        <li>
            <a href="${escapeHtml(item.sourceUrl)}" target="_blank" rel="noopener noreferrer">${escapeHtml(item.title)}</a>
            — ${escapeHtml(item.credit)}.
        </li>
    `).join('');
}

export function prefersReducedMotion() {
    return typeof window !== 'undefined'
        && typeof window.matchMedia === 'function'
        && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}

export function initScrollReveal() {
    if (typeof document === 'undefined') return;

    const targets = document.querySelectorAll(
        '.reveal, .fact-card, .feature-card, .route-step-card, .gallery-card'
    );
    if (!targets.length) return;

    if (prefersReducedMotion() || typeof IntersectionObserver === 'undefined') {
        targets.forEach(el => el.classList.add('revealed'));
        return;
    }

    const observer = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
            if (!entry.isIntersecting) return;
            const el = entry.target;
            el.classList.add('revealed');
            el.addEventListener('transitionend', () => { el.style.transitionDelay = ''; }, { once: true });
            obs.unobserve(el);
        });
    }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });

    targets.forEach(el => {
        const parent = el.parentElement;
        if (parent) {
            const group = Array.from(parent.children).filter(child =>
                child.classList.contains('reveal')
                || child.classList.contains('fact-card')
                || child.classList.contains('feature-card')
                || child.classList.contains('route-step-card')
                || child.classList.contains('gallery-card')
            );
            const position = Math.max(group.indexOf(el), 0);
            el.style.transitionDelay = `${Math.min(position, 7) * 90}ms`;
        }
        observer.observe(el);
    });
}

export function initStatCounters() {
    if (typeof document === 'undefined') return;

    const counters = document.querySelectorAll('.stat-num[data-count]');
    if (!counters.length) return;

    const formatNumber = value => {
        try {
            return value.toLocaleString('en-IN');
        } catch {
            return String(value);
        }
    };

    const runCounter = el => {
        const target = parseInt(el.dataset.count, 10);
        if (Number.isNaN(target)) return;
        const prefix = el.dataset.prefix || '';
        const suffix = el.dataset.suffix || '';

        if (prefersReducedMotion() || typeof requestAnimationFrame !== 'function' || typeof performance === 'undefined') {
            el.textContent = `${prefix}${formatNumber(target)}${suffix}`;
            return;
        }

        const duration = 1600;
        const startTime = performance.now();
        const tick = now => {
            const progress = Math.min((now - startTime) / duration, 1);
            const eased = 1 - Math.pow(1 - progress, 3);
            el.textContent = `${prefix}${formatNumber(Math.round(target * eased))}${suffix}`;
            if (progress < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
    };

    if (typeof IntersectionObserver === 'undefined') {
        counters.forEach(runCounter);
        return;
    }

    const counterObserver = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
            if (!entry.isIntersecting) return;
            runCounter(entry.target);
            obs.unobserve(entry.target);
        });
    }, { threshold: 0.4 });

    counters.forEach(counter => counterObserver.observe(counter));
}

export function initLightbox(gallery = HARISCHANDRAGAD_TREK_DATA.gallery) {
    if (typeof document === 'undefined') return null;

    const lightbox = document.getElementById('glt-lightbox');
    const grid = document.getElementById('gallery-grid');
    if (!lightbox || !grid || !Array.isArray(gallery) || !gallery.length) return null;

    const imageEl = document.getElementById('glt-lightbox-image');
    const titleEl = document.getElementById('glt-lightbox-title');
    const captionEl = document.getElementById('glt-lightbox-caption');
    const creditEl = document.getElementById('glt-lightbox-credit');
    const closeBtn = document.getElementById('glt-lightbox-close');
    const prevBtn = document.getElementById('glt-lightbox-prev');
    const nextBtn = document.getElementById('glt-lightbox-next');

    let currentIndex = 0;
    let hideTimer = null;

    const showItem = index => {
        currentIndex = (index + gallery.length) % gallery.length;
        const item = gallery[currentIndex];
        imageEl.src = item.image;
        imageEl.alt = item.title;
        titleEl.textContent = item.title;
        captionEl.textContent = item.caption;
        creditEl.textContent = item.credit;
        creditEl.href = item.sourceUrl;
    };

    const openLightbox = index => {
        clearTimeout(hideTimer);
        showItem(index);
        lightbox.hidden = false;
        if (typeof requestAnimationFrame === 'function') {
            requestAnimationFrame(() => lightbox.classList.add('open'));
        } else {
            lightbox.classList.add('open');
        }
        document.body.style.overflow = 'hidden';
        closeBtn.focus();
    };

    const closeLightbox = () => {
        lightbox.classList.remove('open');
        document.body.style.overflow = '';
        hideTimer = setTimeout(() => { lightbox.hidden = true; }, 280);
    };

    grid.addEventListener('click', event => {
        const card = event.target.closest('.gallery-card');
        if (!card || event.target.closest('.gallery-credit')) return;
        openLightbox(parseInt(card.dataset.index, 10) || 0);
    });

    grid.addEventListener('keydown', event => {
        if (event.key !== 'Enter' && event.key !== ' ') return;
        const card = event.target.closest('.gallery-card');
        if (!card) return;
        event.preventDefault();
        openLightbox(parseInt(card.dataset.index, 10) || 0);
    });

    closeBtn.addEventListener('click', closeLightbox);
    prevBtn.addEventListener('click', () => showItem(currentIndex - 1));
    nextBtn.addEventListener('click', () => showItem(currentIndex + 1));

    lightbox.addEventListener('click', event => {
        if (event.target === lightbox) closeLightbox();
    });

    document.addEventListener('keydown', event => {
        if (lightbox.hidden) return;
        if (event.key === 'Escape') closeLightbox();
        if (event.key === 'ArrowLeft') showItem(currentIndex - 1);
        if (event.key === 'ArrowRight') showItem(currentIndex + 1);
    });

    return { openLightbox, closeLightbox, showItem };
}

document.addEventListener('DOMContentLoaded', () => {
    const factsGrid = document.getElementById('facts-grid');
    const featuresGrid = document.getElementById('features-grid');
    const routesGrid = document.getElementById('routes-grid');
    const galleryGrid = document.getElementById('gallery-grid');
    const creditsList = document.getElementById('credits-list');

    if (factsGrid) renderTrekFacts(HARISCHANDRAGAD_TREK_DATA.facts, factsGrid);
    if (featuresGrid) renderNaturalFeatures(HARISCHANDRAGAD_TREK_DATA.naturalFeatures, featuresGrid);
    if (routesGrid) renderRouteStages(HARISCHANDRAGAD_TREK_DATA.routeStages, routesGrid);
    if (galleryGrid) renderGallery(HARISCHANDRAGAD_TREK_DATA.gallery, galleryGrid);
    if (creditsList) renderImageCredits(HARISCHANDRAGAD_TREK_DATA.gallery, creditsList);

    initScrollReveal();
    initStatCounters();
    initLightbox();
});
