/* ============ DATA ============ */
const STATS = [
  { value: 901, suffix: " km²", label: "Dry Season Area" },
  { value: 3000, suffix: "+ km²", label: "Monsoon Spread" },
  { value: 200000, suffix: "+", label: "Winter Birds" },
  { value: 67, suffix: "+", label: "Fish Species" }
];

const HOTSPOTS = [
  { id: "atapaka", name: "Atapaka Sanctuary", x: 340, y: 250, type: "sanctuary",
    desc: "The famous Spot-billed Pelican breeding colony. Thousands of pelicans nest here every monsoon, making it one of South India's premier bird sanctuaries.",
    tags: ["Pelican colony", "Breeding ground", "Protected"] },
  { id: "bhimanapalli", name: "Bhimanapalli Temple Island", x: 520, y: 200, type: "cultural",
    desc: "An ancient Shiva temple on an island in the middle of the lake. Accessible only by boat during high-water months.",
    tags: ["Island temple", "Heritage", "Boat access"] },
  { id: "uppumudi", name: "Uppumudi Lighthouse", x: 650, y: 330, type: "viewpoint",
    desc: "Historic observation point where the Upputeru channel drains the lake into the Bay of Bengal. Panoramic views of the lake-sea confluence.",
    tags: ["Sea confluence", "Lighthouse", "Panorama"] },
  { id: "kailash", name: "Kailash Observation Tower", x: 220, y: 180, type: "viewpoint",
    desc: "Newly built birdwatching tower offering elevated views of reed beds and migratory flocks at dawn and dusk.",
    tags: ["Birdwatching", "New facility", "Dawn tours"] },
  { id: "lanja", name: "Lanja Gudi Hill", x: 450, y: 360, type: "cultural",
    desc: "Hilltop temple with legendary views over the lake. Local folklore links it to the Ramayana era.",
    tags: ["Hilltop", "Folklore", "Views"] },
  { id: "konaseema", name: "Konaseema Fishing Villages", x: 180, y: 300, type: "village",
    desc: "Traditional fishing communities whose livelihoods depend on the lake's seasonal rhythms. Known for country-boat craftsmanship.",
    tags: ["Livelihoods", "Craftsmanship", "Culture"] }
];

const BIRDS = [
  { name: "Spot-billed Pelican", sci: "Pelecanus philippensis", icon: "🦢", type: "resident", status: "Near Threatened",
    desc: "The lake's signature species. Breeds in large colonies at Atapaka from July to December.",
    tags: ["Breeding", "Colonial", "Iconic"], accent: "var(--kingfisher)" },
  { name: "Painted Stork", sci: "Mycteria leucocephala", icon: "🦩", type: "resident", status: "Near Threatened",
    desc: "Large wading bird with striking pink-tipped wings. Nests alongside pelicans in mixed colonies.",
    tags: ["Wader", "Colonial", "Colorful"], accent: "var(--sunset)" },
  { name: "Greater Flamingo", sci: "Phoenicopterus roseus", icon: "🦩", type: "migratory", status: "Least Concern",
    desc: "Winter visitor arriving from Central Asia. Thousands gather in shallow lake edges.",
    tags: ["Winter", "Pink", "Flocks"], accent: "#f4a5b5" },
  { name: "Northern Pintail", sci: "Anas acuta", icon: "🦆", type: "migratory", status: "Least Concern",
    desc: "Elegant dabbling duck migrating from Siberia. One of the most abundant winter visitors.",
    tags: ["Siberia", "Dabbler", "Abundant"], accent: "var(--kingfisher)" },
  { name: "Garganey", sci: "Spatula querquedula", icon: "🦆", type: "migratory", status: "Least Concern",
    desc: "Small migratory duck breeding in Europe and wintering in Indian wetlands.",
    tags: ["Europe", "Small", "Dabbler"], accent: "var(--reed)" },
  { name: "Black-tailed Godwit", sci: "Limosa limosa", icon: "🦢", type: "migratory", status: "Near Threatened",
    desc: "Long-billed wader migrating from Siberian and Alaskan breeding grounds.",
    tags: ["Wader", "Long-billed", "Long-distance"], accent: "var(--mud)" },
  { name: "Pied Kingfisher", sci: "Ceryle rudis", icon: "🐦", type: "resident", status: "Least Concern",
    desc: "Black-and-white kingfisher that hovers before diving for fish. Common year-round.",
    tags: ["Resident", "Diving", "Common"], accent: "var(--kingfisher)" },
  { name: "Glossy Ibis", sci: "Plegadis falcinellus", icon: "🦢", type: "resident", status: "Least Concern",
    desc: "Dark-plumaged ibis with iridescent sheen. Forages in flocks across marshes.",
    tags: ["Marshes", "Flocks", "Iridescent"], accent: "var(--reed-deep)" },
  { name: "Eurasian Spoonbill", sci: "Platalea leucorodia", icon: "🦢", type: "migratory", status: "Least Concern",
    desc: "Distinctive spoon-shaped bill used for sweeping through shallow water to find prey.",
    tags: ["Winter", "Spoon-bill", "Wader"], accent: "var(--kingfisher)" },
  { name: "Grey Heron", sci: "Ardea cinerea", icon: "🦢", type: "resident", status: "Least Concern",
    desc: "Tall, patient hunter standing motionless in shallows. Present year-round.",
    tags: ["Resident", "Patient", "Tall"], accent: "var(--muted)" },
  { name: "White-breasted Waterhen", sci: "Amaurornis phoenicurus", icon: "🐦", type: "resident", status: "Least Concern",
    desc: "Shy rail skulking in reed beds. More often heard than seen.",
    tags: ["Reeds", "Shy", "Resident"], accent: "var(--reed)" },
  { name: "Ferruginous Duck", sci: "Aythya nyroca", icon: "🦆", type: "migratory", status: "Near Threatened",
    desc: "Deep-diving duck, a scarce winter visitor preferring open water areas.",
    tags: ["Diving", "Scarce", "Winter"], accent: "var(--sunset)" }
];

const FISH = [
  { name: "Catla", sci: "Catla catla", icon: "🐟", desc: "Large surface-feeding carp, major commercial catch of Kolleru." },
  { name: "Rohu", sci: "Labeo rohita", icon: "🐟", desc: "Column-feeding carp, second most important commercial species." },
  { name: "Mrigal", sci: "Cirrhinus mrigala", icon: "🐟", desc: "Bottom-feeding carp completing the major carp trio of Indian aquaculture." },
  { name: "Kolleru Prawn", sci: "Macrobrachium rosenbergii", icon: "🦐", desc: "Giant freshwater prawn, prized export commodity from the lake." },
  { name: "Hilsa", sci: "Tenualosa ilisha", icon: "🐟", desc: "Anadromous fish migrating from Bay of Bengal to spawn in lake feeders." },
  { name: "Wallago Catfish", sci: "Wallago attu", icon: "🐟", desc: "Large predatory catfish, apex predator of Kolleru's aquatic food web." }
];

const THREATS = [
  { icon: "🏭", title: "Aqua-culture Encroachment", desc: "Over 300 km² of the lake has been illegally converted into prawn and fish ponds, destroying natural reed beds." },
  { icon: "🌾", title: "Agricultural Runoff", desc: "Pesticides and fertilizers from surrounding paddy fields cause eutrophication, choking native vegetation." },
  { icon: "🏗️", title: "Bunding & Reclamation", desc: "Artificial bunds built to create polders have disrupted natural flood dynamics and fish migration routes." },
  { icon: "🏭", title: "Industrial Effluents", desc: "Upstream industries discharge untreated waste into feeder streams, degrading water quality." }
];

const CONSERVATION = [
  { year: "1999", title: "Wildlife Sanctuary Declared", desc: "Kolleru declared a wildlife sanctuary under the Wildlife Protection Act, 1972." },
  { year: "2002", title: "Ramsar Site Designation", desc: "Recognized as a Wetland of International Importance (Ramsar Site #1209)." },
  { year: "2006", title: "Supreme Court Intervention", desc: "Apex Court ordered demolition of illegal aquaculture tanks and restoration of lake area." },
  { year: "2015", title: "National Wetland Conservation", desc: "Included under NWCP with funding for habitat restoration and community engagement." },
  { year: "2022–Present", title: "Community Restoration", desc: "Local fishing communities lead reed-bed restoration and sustainable harvest practices." }
];

const GALLERY = [
  { img: "https://images.unsplash.com/photo-1551085254-e96b210db58a?w=900&q=70", title: "Pelican Colony", desc: "Spot-billed Pelicans breeding at Atapaka sanctuary." },
  { img: "https://images.unsplash.com/photo-1552083375-1447ce886488?w=900&q=70", title: "Lake at Dawn", desc: "Morning mist rising over Kolleru's reed beds." },
  { img: "https://images.unsplash.com/photo-1583212292454-1fe6229603b7?w=900&q=70", title: "Flamingo Flock", desc: "Greater Flamingos feeding in shallow waters." },
  { img: "https://images.unsplash.com/photo-1518467166778-b88f37362005?w=900&q=70", title: "Fishermen's Boats", desc: "Traditional country boats used by local fishing communities." },
  { img: "https://images.unsplash.com/photo-1559060017-445fb9722f2a?w=900&q=70", title: "Reed Beds", desc: "Phragmites reed beds — critical habitat for waterfowl." },
  { img: "https://images.unsplash.com/photo-1544923408-75c5cef320bb?w=900&q=70", title: "Painted Stork", desc: "Painted Stork foraging in the shallows." }
];

const QUIZ = [
  { q: "Pick your perfect morning:", opts: [
    { text: "🌅 Standing still, watching for movement", tags: { heron: 3, kingfisher: 1 } },
    { text: "🤿 Diving head-first into the action", tags: { kingfisher: 4 } },
    { text: "👯 Gathering with friends and family", tags: { pelican: 4, flamingo: 2 } },
    { text: "✈️ Arriving fresh from a long journey", tags: { flamingo: 3, pintail: 3 } }
  ]},
  { q: "Your dining style:", opts: [
    { text: "🎯 Quick and precise — one perfect catch", tags: { kingfisher: 4 } },
    { text: "🥄 Sweeping and filtering", tags: { spoonbill: 4, flamingo: 2 } },
    { text: "🐟 Patient and deep-diving", tags: { cormorant: 3, duck: 2 } },
    { text: "🤝 Sharing meals in large groups", tags: { pelican: 4, stork: 2 } }
  ]},
  { q: "Your home vibe:", opts: [
    { text: "🌾 Hidden in the reeds — peace and privacy", tags: { waterhen: 4, heron: 2 } },
    { text: "🏝️ Open water with a crowd", tags: { pelican: 3, duck: 2 } },
    { text: "🌳 Perched high, watching all", tags: { kingfisher: 3, eagle: 2 } },
    { text: "🏖️ Shallow edges — the border zone", tags: { flamingo: 3, stork: 2 } }
  ]},
  { q: "Your travel philosophy:", opts: [
    { text: "🏡 Stay home year-round, know every corner", tags: { kingfisher: 3, heron: 3 } },
    { text: "✈️ Long-haul flights, seasonal migration", tags: { flamingo: 4, pintail: 3 } },
    { text: "🛶 Short hops between wetlands", tags: { duck: 3, waterhen: 2 } },
    { text: "🌐 Global citizen, everywhere is home", tags: { godwit: 4 } }
  ]},
  { q: "What defines you most:", opts: [
    { text: "🎨 Striking colours and elegance", tags: { flamingo: 4, stork: 2 } },
    { text: "🎯 Precision and skill", tags: { kingfisher: 4 } },
    { text: "👨‍👩‍👧 Family and community", tags: { pelican: 4 } },
    { text: "🧘 Patience and stillness", tags: { heron: 4 } }
  ]}
];

const BIRD_MATCHES = {
  pelican:   { emoji: "🦢", name: "Spot-billed Pelican", desc: "A community-builder! Like Kolleru's pelicans, you thrive in groups and value shared experiences." },
  flamingo:  { emoji: "🦩", name: "Greater Flamingo", desc: "A graceful globetrotter! You bring colour wherever you go and aren't afraid of long journeys." },
  kingfisher:{ emoji: "🐦", name: "Pied Kingfisher", desc: "Sharp and decisive! Like the kingfisher's dive, you strike precisely when opportunity appears." },
  heron:     { emoji: "🦢", name: "Grey Heron", desc: "Patient and observant! You see what others miss and strike only when the moment is perfect." },
  duck:      { emoji: "🦆", name: "Northern Pintail", desc: "Adaptable traveller! You move with the seasons, always finding the right place at the right time." },
  stork:     { emoji: "🦩", name: "Painted Stork", desc: "Elegant and social! You bring beauty to every gathering and value your colony." },
  spoonbill: { emoji: "🦢", name: "Eurasian Spoonbill", desc: "Unique approach! Like your spoon-shaped bill, you have a distinctive way of taking on the world." },
  waterhen:  { emoji: "🐦", name: "White-breasted Waterhen", desc: "Quiet and thoughtful! You prefer the reeds to the spotlight, but your voice carries far." },
  cormorant: { emoji: "🐦", name: "Great Cormorant", desc: "Deep diver! You're not afraid to go below the surface to find what matters." },
  godwit:    { emoji: "🦢", name: "Black-tailed Godwit", desc: "Epic traveller! You cover vast distances with grace, bridging worlds in a single flight." }
};

const FACTS = [
  { text: "Kolleru Lake spans 901 km² in dry season but swells to over 3,000 km² during monsoon — India's largest seasonal lake.", source: "KCHRA Studies" },
  { text: "The lake sits between two mighty rivers — Krishna and Godavari — acting as a natural flood-balancing basin for coastal Andhra.", source: "Ramsar Information Sheet" },
  { text: "Atapaka hosts one of India's largest breeding colonies of Spot-billed Pelicans, with over 5,000 pairs nesting annually.", source: "Bombay Natural History Society" },
  { text: "Over 200,000 waterfowl winter at Kolleru, arriving from as far as Siberia, Central Asia, and Eastern Europe.", source: "Asian Waterbird Census" },
  { text: "Kolleru's 'floating islands' — masses of vegetation called 'tangi' — provide unique microhabitats for fish and birds.", source: "Andhra Pradesh Biodiversity Board" },
  { text: "The lake supports 67+ fish species and sustains over 30,000 fishing families in surrounding villages.", source: "Fisheries Department AP" },
  { text: "In 2006, the Supreme Court of India ordered demolition of over 600 illegal aquaculture tanks to restore the lake.", source: "Supreme Court Judgement" },
  { text: "Kolleru's Upputeru outflow channel drains to the Bay of Bengal — making it a critical link between freshwater and marine ecosystems.", source: "Coastal Zone Studies" },
  { text: "The lake was designated a Ramsar Site (#1209) in 2002, recognizing its international importance for migratory waterbirds.", source: "Ramsar Convention" }
];

const MONTHS = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
const SEASONS = ["Winter Peak", "Winter Peak", "Winter Decline", "Summer", "Pre-monsoon", "Monsoon Start", "Breeding Peak", "Breeding Peak", "Breeding Peak", "Migration Arrives", "Migration Peak", "Winter Peak"];

// Birds per month (in thousands)
const BIRD_POPULATION = {
  resident:  [30, 30, 30, 35, 40, 50, 80, 100, 100, 80, 40, 30],
  migratory: [120, 100, 60, 10, 0, 0, 0, 5, 15, 60, 130, 150],
  breeding:  [0, 0, 0, 0, 5, 20, 80, 100, 100, 60, 20, 5]
};

const MONTH_CAPTIONS = [
  "Winter visitors from Siberia, Central Asia, and Europe are at their peak.",
  "Peak migratory season continues; flamingos and ducks fill the lake.",
  "Winter birds begin their long journey home; residents settle in.",
  "Summer — the lake shrinks, fish concentrate, herons patrol the shallows.",
  "Pre-monsoon heat; waterhen and kingfishers dominate the reed beds.",
  "Monsoon arrives! Waters rise rapidly, transforming the landscape.",
  "Breeding season peaks — pelicans and storks build nests at Atapaka.",
  "Chicks hatch across the lake; the sanctuary buzzes with activity.",
  "Juvenile birds take their first flights; fish breeding peaks.",
  "First winter migrants arrive from the north — early pintails and godwits.",
  "Migration peak — thousands of waterfowl arrive daily from Eurasia.",
  "Winter crowds at maximum — the lake hosts 200,000+ birds."
];

const REDUCED = matchMedia("(prefers-reduced-motion: reduce)").matches;

/* ============ STATE ============ */
const state = {
  birdType: "all",
  birdQuery: "",
  activeHotspot: null,
  monthIdx: 0,
  quizStep: 0,
  quizScores: {},
  factIdx: 0
};

/* ============ HELPERS ============ */
const $ = (id) => document.getElementById(id);
const escapeHtml = (v) => String(v).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;");

function countUp(el, target, dur = 1400) {
  if (REDUCED) { el.textContent = target.toLocaleString("en-IN"); return; }
  const t0 = performance.now();
  (function step(t) {
    const p = Math.min(1, (t - t0) / dur);
    const eased = 1 - Math.pow(1 - p, 3);
    el.textContent = Math.round(target * eased).toLocaleString("en-IN");
    if (p < 1) requestAnimationFrame(step);
  })(t0);
}

/* ============ HERO ============ */
function renderStats() {
  const grid = $("hero-stats");
  grid.innerHTML = STATS.map((s) => `
    <div class="stat-card">
      <strong data-target="${s.value}">0</strong>
      <span>${escapeHtml(s.label)}</span>
    </div>
  `).join("");
  grid.querySelectorAll("[data-target]").forEach((el) => countUp(el, +el.dataset.target));
}

/* ============ MAP ============ */
function renderMap() {
  const markers = $("hotspot-markers");
  markers.innerHTML = HOTSPOTS.map((h) => `
    <g class="hotspot" data-id="${h.id}" transform="translate(${h.x}, ${h.y})">
      <circle r="10"/>
      <text y="-16" text-anchor="middle">${escapeHtml(h.name.split(" ")[0])}</text>
    </g>
  `).join("");

  markers.querySelectorAll(".hotspot").forEach((m) => {
    m.addEventListener("click", () => selectHotspot(m.dataset.id));
    m.addEventListener("mouseenter", (e) => showHotspotTip(m.dataset.id, e));
    m.addEventListener("mouseleave", () => $("map-tooltip").hidden = true);
  });

  const list = $("hotspot-list");
  list.innerHTML = HOTSPOTS.map((h) => `
    <article class="hotspot-card" data-id="${h.id}">
      <h4>${escapeHtml(h.name)}</h4>
      <p>${escapeHtml(h.desc.slice(0, 80))}...</p>
    </article>
  `).join("");
  list.querySelectorAll(".hotspot-card").forEach((c) => {
    c.addEventListener("click", () => selectHotspot(c.dataset.id));
  });
}

function selectHotspot(id) {
  const h = HOTSPOTS.find((x) => x.id === id);
  if (!h) return;
  state.activeHotspot = id;

  document.querySelectorAll(".hotspot").forEach((m) => m.classList.toggle("active", m.dataset.id === id));

  $("map-info").innerHTML = `
    <h3>${escapeHtml(h.name)}</h3>
    <p>${escapeHtml(h.desc)}</p>
    <div class="meta-row">${h.tags.map((t) => `<span class="meta-tag">${escapeHtml(t)}</span>`).join("")}</div>
  `;
}

function showHotspotTip(id, e) {
  const h = HOTSPOTS.find((x) => x.id === id);
  if (!h) return;
  const tip = $("map-tooltip");
  tip.innerHTML = `<strong>${escapeHtml(h.name)}</strong><small>${escapeHtml(h.type)}</small>`;
  tip.hidden = false;
  const rect = $("map-canvas").getBoundingClientRect();
  tip.style.left = (e.clientX - rect.left + 15) + "px";
  tip.style.top = (e.clientY - rect.top - 10) + "px";
}

/* ============ BIRDS ============ */
function renderBirds() {
  const q = state.birdQuery.toLowerCase();
  const visible = BIRDS.filter((b) => {
    const typeOk = state.birdType === "all" ||
      (state.birdType === "threatened" && b.status !== "Least Concern") ||
      b.type === state.birdType;
    if (!typeOk) return false;
    if (!q) return true;
    return (b.name + b.sci + b.desc).toLowerCase().includes(q);
  });

  $("birds-grid").innerHTML = visible.map((b) => `
    <article class="bird-card" style="--accent:${b.accent}">
      <span class="bird-icon">${b.icon}</span>
      <h3>${escapeHtml(b.name)}</h3>
      <span class="sci-name">${escapeHtml(b.sci)}</span>
      <p>${escapeHtml(b.desc)}</p>
      <div class="bird-tags">
        <span class="bird-tag">${escapeHtml(b.type)}</span>
        <span class="bird-tag ${b.status !== "Least Concern" ? "threat" : ""}">${escapeHtml(b.status)}</span>
      </div>
    </article>
  `).join("");
}

/* ============ MIGRATION ============ */
function renderMigration() {
  const chart = $("migration-chart");
  chart.innerHTML = "";

  BIRDS.forEach((b, i) => {
    const pop = (b.type === "migratory" ? BIRD_POPULATION.migratory : BIRD_POPULATION.resident)[state.monthIdx];
    const isHighlight = pop >= 80;
    const group = document.createElement("div");
    group.className = "bar-group";
    group.innerHTML = `
      <div class="bar ${isHighlight ? "highlight" : ""}" style="height:${Math.min(180, pop * 1.5)}px" title="${escapeHtml(b.name)}: ${pop}K"></div>
      <span class="bar-label">${b.icon}</span>
    `;
    chart.appendChild(group);
  });

  $("current-month").textContent = MONTHS[state.monthIdx];
  $("current-season").textContent = SEASONS[state.monthIdx];
  $("migration-caption").textContent = MONTH_CAPTIONS[state.monthIdx];
}

/* ============ FISH ============ */
function renderFish() {
  $("fish-grid").innerHTML = FISH.map((f) => `
    <article class="fish-card">
      <span class="fish-icon">${f.icon}</span>
      <h3>${escapeHtml(f.name)}</h3>
      <span class="sci-name">${escapeHtml(f.sci)}</span>
      <p>${escapeHtml(f.desc)}</p>
    </article>
  `).join("");
}

/* ============ CONSERVATION ============ */
function renderConservation() {
  $("threats-grid").innerHTML = THREATS.map((t) => `
    <article class="threat-card">
      <span class="threat-icon">${t.icon}</span>
      <h3>${escapeHtml(t.title)}</h3>
      <p>${escapeHtml(t.desc)}</p>
    </article>
  `).join("");

  $("conservation-timeline").innerHTML = CONSERVATION.map((c) => `
    <div class="timeline-item">
      <div class="timeline-year">${escapeHtml(c.year)}</div>
      <div class="timeline-content">
        <h4>${escapeHtml(c.title)}</h4>
        <p>${escapeHtml(c.desc)}</p>
      </div>
    </div>
  `).join("");
}

/* ============ GALLERY ============ */
function renderGallery() {
  $("gallery-grid").innerHTML = GALLERY.map((g, i) => `
    <div class="gallery-item" data-idx="${i}">
      <img src="${g.img}" alt="${escapeHtml(g.title)}" loading="lazy">
      <div class="gallery-overlay">
        <h4>${escapeHtml(g.title)}</h4>
        <p>${escapeHtml(g.desc.slice(0, 50))}...</p>
      </div>
    </div>
  `).join("");

  $("gallery-grid").querySelectorAll(".gallery-item").forEach((item) => {
    item.addEventListener("click", () => openLightbox(+item.dataset.idx));
  });
}

function openLightbox(idx) {
  const g = GALLERY[idx];
  if (!g) return;
  $("lightbox-img").src = g.img;
  $("lightbox-title").textContent = g.title;
  $("lightbox-desc").textContent = g.desc;
  $("lightbox").hidden = false;
  document.body.style.overflow = "hidden";
}

function closeLightbox() {
  $("lightbox").hidden = true;
  document.body.style.overflow = "";
}

/* ============ QUIZ ============ */
function renderQuiz() {
  const panel = $("quiz-panel");

  if (state.quizStep >= QUIZ.length) {
    const scores = state.quizScores;
    const winner = Object.entries(scores).sort((a, b) => b[1] - a[1])[0];
    const match = BIRD_MATCHES[winner?.[0]] || BIRD_MATCHES.pelican;
    panel.innerHTML = `
      <div class="quiz-result">
        <div class="match-emoji">${match.emoji}</div>
        <h3>You are</h3>
        <div class="match-name">${escapeHtml(match.name)}</div>
        <p>${escapeHtml(match.desc)}</p>
        <button class="btn-primary" id="quiz-restart">Try Again</button>
      </div>
    `;
    $("quiz-restart").addEventListener("click", () => {
      state.quizStep = 0; state.quizScores = {}; renderQuiz();
    });
    return;
  }

  const q = QUIZ[state.quizStep];
  panel.innerHTML = `
    <div class="quiz-progress">
      <div class="quiz-progress-bar" style="width:${(state.quizStep / QUIZ.length) * 100}%"></div>
    </div>
    <h3 class="quiz-question">${escapeHtml(q.q)}</h3>
    <div class="quiz-options">
      ${q.opts.map((o, i) => `<button class="quiz-option" data-idx="${i}">${escapeHtml(o.text)}</button>`).join("")}
    </div>
  `;

  panel.querySelectorAll(".quiz-option").forEach((btn) => {
    btn.addEventListener("click", () => {
      const opt = q.opts[+btn.dataset.idx];
      Object.entries(opt.tags).forEach(([k, s]) => {
        state.quizScores[k] = (state.quizScores[k] || 0) + s;
      });
      btn.classList.add("selected");
      setTimeout(() => { state.quizStep++; renderQuiz(); }, 600);
    });
  });
}

/* ============ FACTS ============ */
function renderFact() {
  const f = FACTS[state.factIdx];
  $("fact-text").textContent = f.text;
  $("fact-source").textContent = "— " + f.source;
  $("facts-dots").innerHTML = FACTS.map((_, i) => `<i class="${i === state.factIdx ? "active" : ""}" data-i="${i}"></i>`).join("");
  $("facts-dots").querySelectorAll("i").forEach((d) => {
    d.addEventListener("click", () => { state.factIdx = +d.dataset.i; renderFact(); resetFactTimer(); });
  });
}

let factTimer = null;
function startFactTimer() { factTimer = setInterval(() => { state.factIdx = (state.factIdx + 1) % FACTS.length; renderFact(); }, 6500); }
function resetFactTimer() { clearInterval(factTimer); startFactTimer(); }

/* ============ THEME ============ */
function setupTheme() {
  const btn = $("theme-toggle");
  if (!btn) return;
  const sync = () => { btn.textContent = document.body.classList.contains("light-theme") ? "🌞" : "🌙"; };
  btn.addEventListener("click", () => {
    const isLight = document.body.classList.toggle("light-theme");
    try {
      const store = JSON.parse(localStorage.getItem("iie_storage") || "{}");
      store.theme = isLight ? "light" : "dark";
      localStorage.setItem("iie_storage", JSON.stringify(store));
    } catch {}
    sync();
  });
  sync();
}

/* ============ INIT ============ */
document.addEventListener("DOMContentLoaded", () => {
  setupTheme();
  renderStats();
  renderMap();
  renderBirds();
  renderMigration();
  renderFish();
  renderConservation();
  renderGallery();
  renderQuiz();
  renderFact();
  startFactTimer();

  // Select default hotspot
  selectHotspot("atapaka");

  // Bird filters
  $("bird-filters").addEventListener("click", (e) => {
    const btn = e.target.closest(".chip");
    if (!btn) return;
    $("bird-filters").querySelectorAll(".chip").forEach((c) => c.classList.remove("active"));
    btn.classList.add("active");
    state.birdType = btn.dataset.type;
    renderBirds();
  });

  // Bird search
  let searchT;
  $("bird-search").addEventListener("input", (e) => {
    clearTimeout(searchT);
    searchT = setTimeout(() => { state.birdQuery = e.target.value; renderBirds(); }, 180);
  });

  // Month slider
  $("month-slider").addEventListener("input", (e) => {
    state.monthIdx = +e.target.value;
    renderMigration();
  });

  // Lightbox
  $("lightbox").addEventListener("click", (e) => {
    if (e.target.closest("[data-close]")) closeLightbox();
  });
  addEventListener("keydown", (e) => {
    if ($("lightbox").hidden) return;
    if (e.key === "Escape") closeLightbox();
  });

  // Facts nav
  document.querySelector(".facts-nav.prev")?.addEventListener("click", () => {
    state.factIdx = (state.factIdx - 1 + FACTS.length) % FACTS.length;
    renderFact(); resetFactTimer();
  });
  document.querySelector(".facts-nav.next")?.addEventListener("click", () => {
    state.factIdx = (state.factIdx + 1) % FACTS.length;
    renderFact(); resetFactTimer();
  });

  // Back to top
  const topBtn = $("btn-top");
  addEventListener("scroll", () => topBtn.classList.toggle("show", scrollY > 600), { passive: true });
  topBtn.addEventListener("click", () => scrollTo({ top: 0, behavior: REDUCED ? "auto" : "smooth" }));
});