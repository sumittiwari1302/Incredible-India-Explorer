export const GREEN_LAKE_TREK_DATA = {
    name: "Green Lake Trek",
    title: "Green Lake Trek — Sikkim",
    state: "Sikkim",
    region: "North Sikkim, Khangchendzonga National Park (UNESCO World Heritage Site)",
    startingPoint: "Lachen (2,750 m), ~120 km from Gangtok",
    maxAltitude: "4,935 m (16,190 ft)",
    distance: "~92 km return",
    duration: "10–12 days from Lachen",
    difficulty: "Moderate to Difficult",
    bestSeason: "April–May (spring) and October–November (autumn)",
    permits: "Inner Line Permit + special trekking permit via Sikkim Tourism; Protected Area Permit for foreign nationals",

    overview: {
        location: "The Green Lake Trek lies in North Sikkim inside Khangchendzonga National Park, a UNESCO Mixed World Heritage Site on the Tibet border. Green Lake itself sits in the upper Zemu valley directly beneath the north face of Mount Kangchenjunga (8,586 m), the world's third-highest mountain.",
        landscape: "The trail climbs from river valleys through dense temperate forest into alpine meadows and glacier moraine, ringed by Kangchenjunga, Siniolchu, Simvo, Kirat Chuli, Nepal Peak, and Tent Peak.",
        heritage: "In the 1920s–30s the Green Lake basin served as the base for early British and German Kangchenjunga expeditions, including teams associated with George Mallory and Paul Bauer."
    },

    facts: [
        {
            title: "Trek Location",
            value: "North Sikkim",
            description: "Set inside Khangchendzonga National Park in the Zemu valley, on the border region with Tibet.",
            icon: "📍"
        },
        {
            title: "Maximum Altitude",
            value: "4,935 m",
            description: "Green Lake sits at roughly 4,935 m (16,190 ft) at the foot of the Zemu Glacier.",
            icon: "⛰️"
        },
        {
            title: "Distance",
            value: "~92 km Return",
            description: "Around 92 km of walking over the round trip from Lachen to Green Lake and back.",
            icon: "🥾"
        },
        {
            title: "Duration",
            value: "10–12 Days",
            description: "Typical itineraries run 10–12 days from Lachen including acclimatisation halts and a buffer day.",
            icon: "🗓️"
        },
        {
            title: "Difficulty",
            value: "Moderate–Difficult",
            description: "A demanding camping trek with long moraine stages above 4,000 m and indistinct trails.",
            icon: "🧗"
        },
        {
            title: "Best Season",
            value: "Apr–May & Oct–Nov",
            description: "Spring brings rhododendron blooms; autumn offers the clearest mountain views after the monsoon.",
            icon: "🌤️"
        },
        {
            title: "Starting Point",
            value: "Lachen (2,750 m)",
            description: "The trailhead is the Lepcha village of Lachen, about six hours' drive from Gangtok.",
            icon: "🚩"
        },
        {
            title: "Permits",
            value: "Restricted Area",
            description: "Special trekking permits arranged through registered operators are mandatory for all trekkers.",
            icon: "🛂"
        }
    ],

    naturalFeatures: [
        {
            title: "Zemu Glacier",
            text: "At about 26 km long, the Zemu is the largest glacier of the Eastern Himalaya and the principal source of the Teesta River. The final approach walks along its high lateral moraine ridges."
        },
        {
            title: "Green Lake",
            text: "A green-hued glacial lake in a wide amphitheatre below Kangchenjunga's north wall, fringed by meadows where expedition camps have rested since the 1930s."
        },
        {
            title: "Forests & Flora",
            text: "Lower valleys hold dense rhododendron, magnolia, oak, and juniper forest, giving way to primulas, blue poppies, and alpine pastures as the treeline recedes."
        },
        {
            title: "Wildlife",
            text: "The national park shelters red panda, snow leopard, Himalayan black bear, blue sheep, and Himalayan monal — sightings are rare but the wilderness feels alive."
        },
        {
            title: "Peak Panorama",
            text: "From the lake basin, trekkers gaze up at Kangchenjunga (8,586 m), Siniolchu (6,888 m), Simvo (6,812 m), Kirat Chuli, Nepal Peak, and Tent Peak."
        },
        {
            title: "River Valleys",
            text: "The Zemu Chu and Thangu Chu carve the valley system; suspension bridges, waterfalls, and landslide-scarred slopes mark the route's lower stages."
        }
    ],

    routeStages: [
        {
            step: 1,
            stage: "Gangtok → Lachen",
            altitude: "1,600 m → 2,750 m",
            description: "A scenic six-hour mountain drive from Gangtok to the Lepcha village of Lachen, the trek's staging point. Permits are verified here before setting out.",
            keyHighlight: "First glimpses of Lama Angden peak above the village."
        },
        {
            step: 2,
            stage: "Lachen → Zema → Tallem",
            altitude: "2,750 m → 3,240 m",
            description: "A short drive to Zema, the confluence of the Zemu Chu and Thangu Chu, where the walk begins. The trail follows the Zemu valley's left bank through broadening forest.",
            keyHighlight: "Trailhead at the Zema river confluence."
        },
        {
            step: 3,
            stage: "Tallem → Jakthang",
            altitude: "3,240 m → 3,430 m",
            description: "A full day inside magnificent Sikkimese forest — climbing over ridges and dropping to a small clearing at Jakthang beside the river.",
            keyHighlight: "Dense rhododendron and magnolia cloud forest."
        },
        {
            step: 4,
            stage: "Jakthang → Yabuk",
            altitude: "3,430 m → 4,040 m",
            description: "The trail crosses the Thombak Chu and works upstream through fading forest to Yabuk, a cold, windy camp near the snout of the Zemu Glacier. An acclimatisation day is often spent here.",
            keyHighlight: "Treeline crossing and first glacier-side camp."
        },
        {
            step: 5,
            stage: "Yabuk → Rest Camp",
            altitude: "4,040 m → 4,725 m",
            description: "Onto the boulder-strewn moraine fields of the Zemu Glacier — a tiring high-altitude desert walk rewarded with views of Siniolchu and Simvo, with Kangchenjunga appearing in the distance.",
            keyHighlight: "Moraine walking with the first great peak views."
        },
        {
            step: 6,
            stage: "Rest Camp → Green Lake",
            altitude: "4,725 m → 4,935 m",
            description: "A short but breathless climb along the moraine ridge to Green Lake, the green glacial basin directly below Kangchenjunga's enormous north face — the expedition's climax.",
            keyHighlight: "Full panorama of Kangchenjunga, Siniolchu, and the Zemu Glacier snout."
        }
    ],

    gallery: [
        {
            title: "Lachen Village",
            caption: "The traditional Lepcha village of Lachen, the starting point of the Green Lake Trek in North Sikkim.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/6/65/Lachen_Sikkim.jpg/960px-Lachen_Sikkim.jpg",
            credit: "Photo: Tushar2486 / Wikimedia Commons / CC BY-SA 4.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Lachen_Sikkim.jpg"
        },
        {
            title: "Kangchenjunga View Point, Lachen",
            caption: "Mountain vistas on the road into Lachen, gateway to the Zemu valley and Khangchendzonga National Park.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/3/36/Kangchenjunga_View_Point%2C_Lachen%2C_Sikkim.jpg/960px-Kangchenjunga_View_Point%2C_Lachen%2C_Sikkim.jpg",
            credit: "Photo: Prabhat114 / Wikimedia Commons / CC BY-SA 4.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Kangchenjunga_View_Point,_Lachen,_Sikkim.jpg"
        },
        {
            title: "Mount Kangchenjunga",
            caption: "The world's third-highest mountain (8,586 m), whose massive north face towers above Green Lake at the head of the Zemu Glacier.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/8/88/Kangchenjunga_from_Thangshing.jpg/960px-Kangchenjunga_from_Thangshing.jpg",
            credit: "Photo: Carsten.nebel / Wikimedia Commons / CC BY-SA 3.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Kangchenjunga_from_Thangshing.jpg"
        },
        {
            title: "Siniolchu Peak",
            caption: "Siniolchu (6,888 m), widely regarded as one of the most beautiful peaks in the Himalaya, flanks the Zemu valley on the approach to Rest Camp.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/2/24/Siniolchu_CharlesFanning.jpg/960px-Siniolchu_CharlesFanning.jpg",
            credit: "Photo: Charles Fanning / Wikimedia Commons / Public domain",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Siniolchu_CharlesFanning.jpg"
        },
        {
            title: "Alpine Valley Meadows",
            caption: "Flower-filled high-altitude valleys like Yumthang mirror the meadow country that frames the lower Green Lake trail each spring.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/d/d7/Yumthang_valley%2C_Lachung_Sikkim_India_2012.jpg/960px-Yumthang_valley%2C_Lachung_Sikkim_India_2012.jpg",
            credit: "Photo: Joginder Pathak / Wikimedia Commons / CC BY-SA 2.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Yumthang_valley,_Lachung_Sikkim_India_2012.jpg"
        },
        {
            title: "High-Altitude Lakes of North Sikkim",
            caption: "North Sikkim's starkly beautiful glacial lakes, such as Gurudongmar, share the same protected high-altitude wilderness as Green Lake.",
            image: "https://upload.wikimedia.org/wikipedia/commons/thumb/3/3a/Gurudongmar_Lake_Sikkim%2C_India.jpg/960px-Gurudongmar_Lake_Sikkim%2C_India.jpg",
            credit: "Photo: Yoghya / Wikimedia Commons / CC BY-SA 4.0",
            sourceUrl: "https://commons.wikimedia.org/wiki/File:Gurudongmar_Lake_Sikkim,_India.jpg"
        }
    ]
};

const FALLBACK_IMAGE = 'data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="400" height="250" viewBox="0 0 100 100"><rect width="100%" height="100%" fill="%231e293b"/><text x="50%" y="55%" fill="%2334d399" font-size="24" text-anchor="middle"></text></svg>';

function escapeHtml(value) {
    return String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');
}

export function renderTrekFacts(facts, container) {
    if (!container || !Array.isArray(facts)) return;

    container.innerHTML = facts.map(fact => `
        <article class="fact-card">
            <div class="fact-icon">${escapeHtml(fact.icon)}</div>
            <div class="fact-value">${escapeHtml(fact.value)}</div>
            <h3 class="fact-title">${escapeHtml(fact.title)}</h3>
            <p class="fact-desc">${escapeHtml(fact.description)}</p>
        </article>
    `).join('');
}

export function renderNaturalFeatures(features, container) {
    if (!container || !Array.isArray(features)) return;

    container.innerHTML = features.map(feature => `
        <article class="feature-card">
            <h3>${escapeHtml(feature.title)}</h3>
            <p>${escapeHtml(feature.text)}</p>
        </article>
    `).join('');
}

export function renderRouteStages(stages, container) {
    if (!container || !Array.isArray(stages)) return;

    container.innerHTML = stages.map(stage => `
        <div class="route-step-card" data-step="${stage.step}">
            <div class="step-badge">Stage ${stage.step}</div>
            <h3 class="step-stage">${escapeHtml(stage.stage)}</h3>
            <div class="step-altitude">📍 Altitude: ${escapeHtml(stage.altitude)}</div>
            <p class="step-desc">${escapeHtml(stage.description)}</p>
            <div class="step-highlight">✨ <strong>Highlight:</strong> ${escapeHtml(stage.keyHighlight)}</div>
        </div>
    `).join('');
}

export function renderGallery(gallery, container) {
    if (!container || !Array.isArray(gallery)) return;

    container.innerHTML = gallery.map((item, index) => `
        <figure class="gallery-card" data-index="${index}" role="button" tabindex="0" aria-label="View larger image: ${escapeHtml(item.title)}">
            <div class="gallery-img-wrapper">
                <img
                    src="${escapeHtml(item.image)}"
                    alt="${escapeHtml(item.title)}"
                    class="gallery-img"
                    loading="lazy"
                    onerror="this.onerror=null; this.src='${FALLBACK_IMAGE}';"
                />
            </div>
            <figcaption class="gallery-caption">
                <h4>${escapeHtml(item.title)}</h4>
                <p>${escapeHtml(item.caption)}</p>
                <a class="gallery-credit" href="${escapeHtml(item.sourceUrl)}" target="_blank" rel="noopener noreferrer">${escapeHtml(item.credit)}</a>
            </figcaption>
        </figure>
    `).join('');
}

export function renderImageCredits(gallery, container) {
    if (!container || !Array.isArray(gallery)) return;

    container.innerHTML = gallery.map(item => `
        <li>
            <a href="${escapeHtml(item.sourceUrl)}" target="_blank" rel="noopener noreferrer">${escapeHtml(item.title)}</a>
            — ${escapeHtml(item.credit)}.
        </li>
    `).join('');
}

export function prefersReducedMotion() {
    return typeof window !== 'undefined'
        && typeof window.matchMedia === 'function'
        && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}

export function initScrollReveal() {
    if (typeof document === 'undefined') return;

    const targets = document.querySelectorAll(
        '.reveal, .fact-card, .feature-card, .route-step-card, .gallery-card'
    );
    if (!targets.length) return;

    if (prefersReducedMotion() || typeof IntersectionObserver === 'undefined') {
        targets.forEach(el => el.classList.add('revealed'));
        return;
    }

    const observer = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
            if (!entry.isIntersecting) return;
            const el = entry.target;
            el.classList.add('revealed');
            el.addEventListener('transitionend', () => { el.style.transitionDelay = ''; }, { once: true });
            obs.unobserve(el);
        });
    }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });

    targets.forEach(el => {
        const parent = el.parentElement;
        if (parent) {
            const group = Array.from(parent.children).filter(child =>
                child.classList.contains('reveal')
                || child.classList.contains('fact-card')
                || child.classList.contains('feature-card')
                || child.classList.contains('route-step-card')
                || child.classList.contains('gallery-card')
            );
            const position = Math.max(group.indexOf(el), 0);
            el.style.transitionDelay = `${Math.min(position, 7) * 90}ms`;
        }
        observer.observe(el);
    });
}

export function initStatCounters() {
    if (typeof document === 'undefined') return;

    const counters = document.querySelectorAll('.stat-num[data-count]');
    if (!counters.length) return;

    const formatNumber = value => {
        try {
            return value.toLocaleString('en-IN');
        } catch {
            return String(value);
        }
    };

    const runCounter = el => {
        const target = parseInt(el.dataset.count, 10);
        if (Number.isNaN(target)) return;
        const prefix = el.dataset.prefix || '';
        const suffix = el.dataset.suffix || '';

        if (prefersReducedMotion() || typeof requestAnimationFrame !== 'function' || typeof performance === 'undefined') {
            el.textContent = `${prefix}${formatNumber(target)}${suffix}`;
            return;
        }

        const duration = 1600;
        const startTime = performance.now();
        const tick = now => {
            const progress = Math.min((now - startTime) / duration, 1);
            const eased = 1 - Math.pow(1 - progress, 3);
            el.textContent = `${prefix}${formatNumber(Math.round(target * eased))}${suffix}`;
            if (progress < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
    };

    if (typeof IntersectionObserver === 'undefined') {
        counters.forEach(runCounter);
        return;
    }

    const counterObserver = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
            if (!entry.isIntersecting) return;
            runCounter(entry.target);
            obs.unobserve(entry.target);
        });
    }, { threshold: 0.4 });

    counters.forEach(counter => counterObserver.observe(counter));
}

export function initLightbox(gallery = GREEN_LAKE_TREK_DATA.gallery) {
    if (typeof document === 'undefined') return null;

    const lightbox = document.getElementById('glt-lightbox');
    const grid = document.getElementById('gallery-grid');
    if (!lightbox || !grid || !Array.isArray(gallery) || !gallery.length) return null;

    const imageEl = document.getElementById('glt-lightbox-image');
    const titleEl = document.getElementById('glt-lightbox-title');
    const captionEl = document.getElementById('glt-lightbox-caption');
    const creditEl = document.getElementById('glt-lightbox-credit');
    const closeBtn = document.getElementById('glt-lightbox-close');
    const prevBtn = document.getElementById('glt-lightbox-prev');
    const nextBtn = document.getElementById('glt-lightbox-next');

    let currentIndex = 0;
    let hideTimer = null;

    const showItem = index => {
        currentIndex = (index + gallery.length) % gallery.length;
        const item = gallery[currentIndex];
        imageEl.src = item.image;
        imageEl.alt = item.title;
        titleEl.textContent = item.title;
        captionEl.textContent = item.caption;
        creditEl.textContent = item.credit;
        creditEl.href = item.sourceUrl;
    };

    const openLightbox = index => {
        clearTimeout(hideTimer);
        showItem(index);
        lightbox.hidden = false;
        if (typeof requestAnimationFrame === 'function') {
            requestAnimationFrame(() => lightbox.classList.add('open'));
        } else {
            lightbox.classList.add('open');
        }
        document.body.style.overflow = 'hidden';
        closeBtn.focus();
    };

    const closeLightbox = () => {
        lightbox.classList.remove('open');
        document.body.style.overflow = '';
        hideTimer = setTimeout(() => { lightbox.hidden = true; }, 280);
    };

    grid.addEventListener('click', event => {
        const card = event.target.closest('.gallery-card');
        if (!card || event.target.closest('.gallery-credit')) return;
        openLightbox(parseInt(card.dataset.index, 10) || 0);
    });

    grid.addEventListener('keydown', event => {
        if (event.key !== 'Enter' && event.key !== ' ') return;
        const card = event.target.closest('.gallery-card');
        if (!card) return;
        event.preventDefault();
        openLightbox(parseInt(card.dataset.index, 10) || 0);
    });

    closeBtn.addEventListener('click', closeLightbox);
    prevBtn.addEventListener('click', () => showItem(currentIndex - 1));
    nextBtn.addEventListener('click', () => showItem(currentIndex + 1));

    lightbox.addEventListener('click', event => {
        if (event.target === lightbox) closeLightbox();
    });

    document.addEventListener('keydown', event => {
        if (lightbox.hidden) return;
        if (event.key === 'Escape') closeLightbox();
        if (event.key === 'ArrowLeft') showItem(currentIndex - 1);
        if (event.key === 'ArrowRight') showItem(currentIndex + 1);
    });

    return { openLightbox, closeLightbox, showItem };
}

document.addEventListener('DOMContentLoaded', () => {
    const factsGrid = document.getElementById('facts-grid');
    const featuresGrid = document.getElementById('features-grid');
    const routesGrid = document.getElementById('routes-grid');
    const galleryGrid = document.getElementById('gallery-grid');
    const creditsList = document.getElementById('credits-list');

    if (factsGrid) renderTrekFacts(GREEN_LAKE_TREK_DATA.facts, factsGrid);
    if (featuresGrid) renderNaturalFeatures(GREEN_LAKE_TREK_DATA.naturalFeatures, featuresGrid);
    if (routesGrid) renderRouteStages(GREEN_LAKE_TREK_DATA.routeStages, routesGrid);
    if (galleryGrid) renderGallery(GREEN_LAKE_TREK_DATA.gallery, galleryGrid);
    if (creditsList) renderImageCredits(GREEN_LAKE_TREK_DATA.gallery, creditsList);

    initScrollReveal();
    initStatCounters();
    initLightbox();
});
