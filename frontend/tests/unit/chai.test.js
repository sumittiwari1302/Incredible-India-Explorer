import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const page=readFileSync(resolve(process.cwd(),"frontend/Chai/index.html"),"utf8"),script=readFileSync(resolve(process.cwd(),"frontend/Chai/chai.js"),"utf8");describe("Chai cultural explorer",()=>{it("documents chai culture and preparations",()=>{["Chai","Masala chai","Cutting chai","Kashmiri noon chai","Sulaimani tea","Serving practices"].forEach(x=>expect(page).toContain(x))});it("contains the interactive regional map",()=>{expect(page).toContain("Simplified map of major Indian tea-growing regions");["assam","darjeeling","dooars","kangra","sikkim","nilgiri","munnar"].forEach(x=>expect(page).toContain(`data-region="${x}"`));expect(script).toContain("function regionSet")});it("includes sources and responsive metadata",()=>{["Tea Board India","Incredible India","FAO",'name="viewport"',"prefers-reduced-motion"].forEach(x=>expect(page).toContain(x))})});